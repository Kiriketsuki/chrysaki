# Chrysaki migration inventory

Generated inventory captured before the ecosystem split.

- Canonical SCSS tokens: **34**
- Downstream supplemental tokens: **2**
- Distinct application-local literals: **251**
- Files containing color literals: **52**

`golden-tokens.json` is the extraction invariant. Core validation compares its resolved output against this snapshot. `token-inventory.json` records non-canonical literals without promoting application-specific mappings into core.
