# Chrysaki integration repository template

Copy this directory into a new public repository. Replace `APPLICATION` and package metadata, then follow `EXTRACTION.md`.

The adapter must consume the pinned core contract and contain no duplicate canonical palette. Generated application output is committed and checked for staleness in CI.
